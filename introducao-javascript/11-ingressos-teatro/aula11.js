const totalCompra = 180;
let desconto = 0;
if (totalCompra > 200) { desconto = 0.15;} else if (totalCompra >= 100) { desconto = 0.05;}
const valorComDesconto + totalCompra  * (1 - desconto);
const frete =  totalCompra > valorComDesconto + frete;

document.getElementById("resltado"). textContent = 'Total a paga: R$ ${totalFinal.fixed(2)}';