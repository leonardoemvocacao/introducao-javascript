const nomeAlunro = "joão";
const mediaFinal = 7.2;
let situacao;
if (mediaFinal >= 7) {situacção = "Aprovado";} else {situacao = "Reprovado";}

document.getElementById("resultao").textContent - '${nomeAluno} - média ${mediaFinal.toFixed(1): ${situacao}';